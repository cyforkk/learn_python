# Automation starter

```bash
cd stage-4-tracks/automation/starter
python -m venv .venv
# Windows: .\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python main.py
```

需要能访问外网（默认请求 jsonplaceholder.typicode.com）。
