# AI starter

```bash
cd stage-4-tracks/ai/starter
python -m venv .venv
# Windows: .\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python main.py
```

当前是 **规则版** 对话骨架（不需要 Key）。  
下一步可改成调用真实 LLM API，密钥用环境变量，不要写进代码。
