# 学习日记：2026-07-11

## 今天学了什么

- 确认仓库结构与学习方式：notes → exercises → `--check` → solutions
- 阶段 0：环境可用（Python 3.10.11）
- 阶段 1 练习 01～05 已补全实现并通过 `--check`

## 写了什么代码

- 路径：`stage-0-setup/hello.py`
- 路径：`stage-1-basics/exercises/ex01`～`ex05`
- 功能：猜数字、计算器、成绩统计、九九表、等级函数

## 关键点回顾（自己再读一遍代码）

- `compare_guess`：用 if 分支返回 low/high/ok
- `calculate`：除零返回 None；非法运算符 raise ValueError
- 字典 `max/min(..., key=scores.get)` 找最高/最低分
- `build_table`：嵌套循环生成九九表行
- `grade_level`：从高到低判断分数段

## 卡在哪里、怎么解决

- （自学时把疑问记这里）

## 明日 / 接下来计划

- [ ] 读完 stage-1 `notes/` 八篇笔记（对照已写代码加深理解）
- [ ] 不看答案，尝试默写 ex01 的 `compare_guess` + 循环
- [ ] 进入 stage-2 第一题 `ex01_todo_json.py`
