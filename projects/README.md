# 个人项目工作区

把你自己的项目放在本目录下，例如：

```
projects/
├── todo-cli/
├── automation-demo/
└── ...
```

每个项目建议包含：

- `README.md` — 如何运行
- `requirements.txt` — 依赖（如有）
- 源代码

不要提交 API Key、密码；使用 `.env`（仓库根目录已 gitignore）。
